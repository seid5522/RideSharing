<resources>

    <!-- Base application theme. -->
    <style name="FlockBrightTheme"  parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/sulu</item>
        <item name="android:colorBackground">@color/sulu</item>
    </style>

    <!-- Flock Color Set Bright -->
    <color name="sugar_kane">#fdfffd</color>
    <color name="dream_scream">#6afa3c</color>
    <color name="alto">#d3d3d3</color>
    <color name="mantis">#73be5b</color>
    <color name="sulu">#a3eb8c</color>

    <style name="CodeFont" parent="@android:style/TextAppearance.Medium">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/sugar_kane</item>
        <item name="android:typeface">monospace</item>
    </style>

    <!-- the theme applied to the application or activity -->
    <style name="FlockActionBarTheme"
        parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionMenuTextColor">@color/sugar_kane</item>
        <item name="android:windowActionBarOverlay">false</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/actionbar_background</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
        parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/sugar_kane</item>
    </style>

    <!-- ActionBar tabs text styles -->
    <style name="MyActionBarTabText"
        parent="@android:style/Widget.Holo.ActionBar.TabText">
        <item name="android:textColor">@color/sugar_kane</item>
    </style>
</resources>
